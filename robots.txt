User-agent: *
Allow: /

# Internal-only OG-image renderer (tools/og-renderer/og-{en,ar,fr}.html).
# Excluded from the deploy artifact by .github/workflows/deploy-pages.yml
# ("Stage public files" step), so well-behaved crawlers won't find it
# anyway. This Disallow is belt-and-suspenders: if any future deploy
# mechanism bypasses the workflow's exclusion (e.g. a manual upload of
# the repo root), crawlers honoring robots.txt still avoid the path.
Disallow: /tools/

Sitemap: https://elshazlyyy.github.io/Awaab-Public/sitemap.xml
